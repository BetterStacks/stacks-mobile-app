import { StyleSheet } from "react-native";
import { scaleHeight } from "shared/utils/design/scale";

export const styles = StyleSheet.create({
  sitesCarouselContainer: {
    marginTop: scaleHeight(11),
  },
});
